{
  "name": "barber-hub-oficial",
  "version": "1.6.0",
  "private": true,
  "description": "Portal, gestão, PWA e API própria em Python do Barber Hub.",
  "scripts": {
    "dev": "vercel dev",
    "check": "node scripts/check-project.js && python scripts/validate_project.py && python -m compileall -q api backend && python -m unittest discover -s tests -v",
    "api:dev": "fastapi dev api/index.py",
    "check:python": "python -m compileall -q api backend"
  },
  "engines": {
    "node": ">=20"
  }
}
