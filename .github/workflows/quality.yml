name: Qualidade e segurança

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      - name: Instalar API
        run: python -m pip install -e .
      - name: Validar API instalada
        run: |
          python -m pip check
          python -I -c "from api.index import app; from backend.services import schedule; assert callable(app)"
      - name: Sincronizar e validar a versão
        run: npm run check
